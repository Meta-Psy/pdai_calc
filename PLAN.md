# План модернизации PDAI Calculator

## Обзор

Текущее состояние: один HTML-файл (~37 КБ), React + Babel Standalone + Tailwind Play CDN.
Цель: SEO-оптимизированный, многоязычный медицинский калькулятор с компактной печатью.

---

## Этап 0. Подготовка инфраструктуры

### 0.1 Миграция на Vite + React (pre-build)
- **Зачем:** Babel Standalone (~3 МБ) и Tailwind CDN убивают производительность и SEO. Без сборки Google видит пустой `<div id="root">`.
- **Что делаем:**
  - `npm create vite@latest pdai-calculator -- --template react`
  - Переносим компонент `PDAICalculator` в `src/App.jsx`
  - Устанавливаем Tailwind CSS через PostCSS (production-build с tree-shaking)
  - SVG-иконки выносим в `src/components/icons/`
  - Итог: вместо ~4 МБ загрузки → ~80-120 КБ, мгновенный рендер

### 0.2 Исправить устаревший React API
- Заменить `ReactDOM.render()` на `createRoot()` (React 18+)

### 0.3 Файловая структура
```
pdai-calculator/
├── public/
│   ├── favicon.ico
│   ├── og-image.png          (1200×627, превью для соцсетей)
│   ├── robots.txt
│   └── sitemap.xml
├── src/
│   ├── components/
│   │   ├── icons/             (SVG-компоненты)
│   │   ├── PatientForm.jsx
│   │   ├── SkinTable.jsx
│   │   ├── ScalpTable.jsx
│   │   ├── MucosaTable.jsx
│   │   ├── ResultsSummary.jsx
│   │   ├── BodyVisualization.jsx
│   │   ├── Recommendations.jsx
│   │   ├── InfoSection.jsx
│   │   └── LanguageSwitcher.jsx
│   ├── i18n/
│   │   ├── ru.json
│   │   ├── en.json
│   │   ├── uz.json
│   │   └── kk.json            (каракалпакский, кириллица)
│   ├── hooks/
│   │   └── useCalculator.js   (логика расчёта PDAI)
│   ├── print.css              (стили печати)
│   ├── App.jsx
│   ├── App.css
│   └── main.jsx
├── index.html
├── package.json
├── tailwind.config.js
├── postcss.config.js
└── vite.config.js
```

---

## Этап 1. SEO-оптимизация (основной акцент)

### 1.1 Семантическая HTML-разметка
- **Зачем:** Google и скринридеры понимают структуру страницы. Влияет на ранжирование.
- **Что делаем:**
  - `<header>` — шапка с названием и переключателем языков
  - `<main>` — основной контент калькулятора
  - `<section>` — каждый блок (данные пациента, кожа, голова, слизистая, результаты)
  - `<footer>` — копирайт и ссылки
  - Правильная иерархия заголовков: `<h1>` (один, название), `<h2>` (секции), `<h3>` (подсекции)
  - `<label htmlFor="id">` связываем с каждым `<input>`/`<select>`

### 1.2 Мета-теги и Open Graph
```html
<!-- Базовые -->
<title>PDAI Calculator — Pemphigus Disease Area Index | Skin Lab Pro</title>
<meta name="description" content="Free online PDAI calculator for dermatologists.
  Assess pemphigus severity with the standardized Pemphigus Disease Area Index score.">
<link rel="canonical" href="https://[domain]/ru/">

<!-- Open Graph (Facebook, LinkedIn, Telegram) -->
<meta property="og:type" content="website">
<meta property="og:title" content="PDAI Calculator — Калькулятор индекса тяжести пузырчатки">
<meta property="og:description" content="Бесплатный онлайн-калькулятор PDAI для дерматологов">
<meta property="og:image" content="https://[domain]/og-image.png">
<meta property="og:url" content="https://[domain]/ru/">
<meta property="og:locale" content="ru_RU">
<meta property="og:locale:alternate" content="en_US">
<meta property="og:locale:alternate" content="uz_UZ">

<!-- Twitter/X Card -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="PDAI Calculator">
<meta name="twitter:description" content="Pemphigus Disease Area Index calculator for healthcare professionals">
<meta name="twitter:image" content="https://[domain]/og-image.png">
```
- Мета-теги будут динамически меняться при смене языка через `react-helmet-async`

### 1.3 Structured Data (Schema.org JSON-LD)
```json
{
  "@context": "https://schema.org",
  "@type": "MedicalWebPage",
  "name": "PDAI Calculator",
  "description": "Pemphigus Disease Area Index calculator",
  "medicalSpecialty": "Dermatology",
  "mainEntity": {
    "@type": "MedicalRiskCalculator",
    "name": "PDAI Score Calculator",
    "description": "Calculates Pemphigus Disease Area Index for disease severity assessment",
    "url": "https://[domain]/"
  },
  "about": {
    "@type": "MedicalCondition",
    "name": "Pemphigus",
    "alternateName": "Пузырчатка"
  },
  "inLanguage": ["ru", "en", "uz", "kk-Cyrl"],
  "isAccessibleForFree": true
}
```

### 1.4 Многоязычный SEO (hreflang)
```html
<link rel="alternate" hreflang="ru" href="https://[domain]/ru/">
<link rel="alternate" hreflang="en" href="https://[domain]/en/">
<link rel="alternate" hreflang="uz" href="https://[domain]/uz/">
<link rel="alternate" hreflang="kk" href="https://[domain]/kk/">
<link rel="alternate" hreflang="x-default" href="https://[domain]/en/">
```
- Каждый язык — свой URL-путь (`/ru/`, `/en/`, `/uz/`, `/kk/`)
- Все hreflang-ссылки двунаправленные (на каждой странице есть ссылки на ВСЕ версии)
- `x-default` — английская версия (для пользователей, чей язык не в списке)

### 1.5 robots.txt и sitemap.xml
**robots.txt:**
```
User-agent: *
Allow: /
Sitemap: https://[domain]/sitemap.xml
```

**sitemap.xml:**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  <url>
    <loc>https://[domain]/ru/</loc>
    <xhtml:link rel="alternate" hreflang="ru" href="https://[domain]/ru/"/>
    <xhtml:link rel="alternate" hreflang="en" href="https://[domain]/en/"/>
    <xhtml:link rel="alternate" hreflang="uz" href="https://[domain]/uz/"/>
    <xhtml:link rel="alternate" hreflang="kk" href="https://[domain]/kk/"/>
  </url>
  <!-- аналогично для /en/, /uz/, /kk/ -->
</urlset>
```

### 1.6 Pre-rendering для SSR-подобного эффекта
- Используем `vite-plugin-prerender` — при билде генерирует статический HTML для каждого маршрута (`/ru/`, `/en/`, `/uz/`, `/kk/`)
- Google сразу видит готовый контент, а не пустой `<div>`
- После загрузки React "оживляет" (hydrate) страницу для интерактивности

### 1.7 Производительность (Core Web Vitals)
- **LCP < 2.0с:** Pre-rendered HTML + минифицированные ресурсы
- **INP < 150мс:** `React.memo` для таблиц, `useCallback` для обработчиков
- **CLS < 0.08:** Зарезервированные размеры для контейнеров, нет layout shift
- `<link rel="preconnect">` для внешних ресурсов (если останутся)
- Favicon: `.ico` + `.svg` + `apple-touch-icon.png`

### 1.8 Дополнительные SEO-элементы
- Атрибут `lang` динамически меняется: `<html lang="ru">` → `<html lang="en">` и т.д.
- `<meta name="robots" content="index, follow">`
- `<meta name="author" content="Skin Lab Pro">`
- Год в копирайте: `© 2024-2026`

---

## Этап 2. Переименование и брендинг

### 2.1 Название
- `Skin Lab Pro` остаётся как бренд
- Заголовок калькулятора → **PDAI Calculator**
- `<h1>`: "PDAI Calculator" (на всех языках название калькулятора не переводится — это международная аббревиатура)
- Подзаголовок `<p>` под `<h1>` — переводится: "Калькулятор индекса тяжести пузырчатки" / "Pemphigus Disease Area Index Calculator" и т.д.
- `<title>`: "PDAI Calculator — Skin Lab Pro"

---

## Этап 3. Мультиязычность

### 3.1 Архитектура i18n
- **Библиотека:** `react-i18next` + `i18next` (стандарт индустрии, поддержка SEO-маршрутов)
- **Файлы переводов:** JSON по одному на язык (`ru.json`, `en.json`, `uz.json`, `kk.json`)
- **Маршрутизация:** `react-router-dom` с путями `/:lang/` → переключение языка меняет URL
- **Хранение выбора:** `localStorage` + определение языка браузера при первом входе

### 3.2 Переключатель языков (LanguageSwitcher)
- **Расположение:** шапка, справа, в одной строке с названием
- **Внешний вид:** флаги + короткий код языка
- **Флаги:**
  - 🇷🇺 RU — русский
  - 🇺🇿 UZ — узбекский
  - 🇬🇧 EN — английский
  - 🇺🇿 КК — каракалпакский (флаг Каракалпакстана или Узбекистана)
- **Реализация флагов:** SVG-иконки или CSS-флаги (через `flag-icons` или инлайн-SVG), НЕ emoji (они по-разному рендерятся на разных ОС)
- **Поведение:** клик по флагу → URL меняется (`/ru/` → `/en/`), контент перерисовывается, `<html lang>` обновляется

### 3.3 Объём переводов
Что переводится (примерный состав каждого JSON):
- Заголовки секций (~10 строк)
- Метки полей пациента (~5 строк)
- Названия анатомических зон кожи (~12 строк)
- Названия зон слизистой (~12 строк)
- Шкалы оценок и инструкции (~25 строк)
- Результаты и степени тяжести (~10 строк)
- Информация о PDAI (~10 строк)
- Кнопки и подсказки (~8 строк)
- Мета-описания для SEO (~5 строк)
- **Итого: ~100 строк на язык**

### 3.4 Языки
| Код | Язык | Письменность | Пример |
|-----|------|-------------|--------|
| `ru` | Русский | Кириллица | Калькулятор индекса тяжести пузырчатки |
| `en` | English | Латиница | Pemphigus Disease Area Index Calculator |
| `uz` | O'zbekcha | Латиница | Pemfigus kasalligi hududi indeksi kalkulyatori |
| `kk` | Қарақалпақша | Кириллица | Пемфигус кеселлиги аймақ индекси калькуляторы |

---

## Этап 4. Печать на 1 страницу

### 4.1 Стратегия: скрытие + компрессия + масштаб

**Фаза A — Скрытие нерелевантного для печати:**
- SVG-визуализации (уже скрыты) ✓
- Жёлтые блоки с инструкциями по шкалам (нужны только при вводе)
- Синий информационный блок о PDAI
- Переключатель языков и кнопки действий
- Шапка с логотипом (заменить на компактную строку: "PDAI Calculator — дата — ФИО")

**Фаза B — Компактные стили печати:**
```css
@media print {
  @page {
    size: A4;
    margin: 5mm 8mm;
  }

  body {
    font-size: 7pt;
    line-height: 1.15;
  }

  h2 { font-size: 9pt; margin: 2px 0; }

  table th, table td {
    padding: 1px 3px;
    font-size: 6.5pt;
  }

  /* Убрать все большие отступы */
  .mb-6, .mb-4, .p-6, .p-8 {
    margin-bottom: 2px;
    padding: 4px;
  }
}
```

**Фаза C — Масштабирование (если ещё не влезает):**
```css
@media print {
  #root {
    transform: scale(0.72);
    transform-origin: top left;
    width: 139%; /* 100/0.72 */
  }
}
```

**Фаза D — Компактная шапка для печати:**
- Вместо полного хедера → одна строка: "PDAI Calculator | [ФИО пациента] | [Дата]"
- Результаты вынести вверх (сразу после данных пациента)

### 4.2 Печатный макет (порядок блоков)
1. Компактная строка-шапка (название + дата)
2. Данные пациента (1 строка на 4 поля)
3. **Результаты PDAI** (перенести наверх! — самое важное)
4. Таблица кожи (компактная)
5. Таблица кожи головы (компактная)
6. Таблица слизистой (компактная)
7. Рекомендации врача (если заполнены)

### 4.3 Ожидаемый результат
- Скрытие инструкций и визуализаций: убирает ~40% высоты
- Компрессия шрифтов и отступов: ещё ~25%
- Масштабирование 0.72: оставшийся контент влезает на 1 страницу A4

---

## Этап 5. Доступность (a11y)

### 5.1 Быстрые улучшения
- `aria-hidden="true"` на декоративные SVG-иконки
- `aria-label` на кнопки-баллы (сейчас просто цифра без контекста)
- `<label htmlFor>` для всех input/select (сейчас label не привязан)
- Контрастность текста в зонах результатов (белый на градиенте)
- Индикация тяжести не только цветом, но и текстом + иконкой (уже частично есть)

---

## Этап 6. Мелкие исправления

- Копирайт: `© 2024` → `© 2024-2026`
- Кнопка PDF: сейчас `window.print()` — то же, что и "Печать". Оставить одну кнопку "Печать / PDF"
- `localStorage`: сохранение данных пациента при случайном обновлении страницы
- SRI-хэши для любых внешних ресурсов (если останутся CDN-зависимости)

---

## Порядок реализации

| # | Этап | Приоритет | Описание |
|---|------|-----------|----------|
| 1 | Этап 0 | Высший | Миграция на Vite, структура проекта |
| 2 | Этап 2 | Высший | Переименование в PDAI Calculator |
| 3 | Этап 3 | Высший | i18n: 4 языка + переключатель с флагами |
| 4 | Этап 1 | Высший | SEO: мета-теги, семантика, Schema.org, hreflang |
| 5 | Этап 4 | Высокий | Печать на 1 страницу A4 |
| 6 | Этап 5 | Средний | Доступность |
| 7 | Этап 6 | Средний | Мелкие доработки |

> **Примечание:** Этапы 0-3 тесно связаны и выполняются вместе —
> нельзя сделать hreflang без маршрутизации, а маршрутизацию без Vite.

---

## Что НЕ входит в план

- Деплой на хостинг (Vercel/Netlify/свой сервер) — обсуждается отдельно
- Создание OG-изображения (1200×627) — нужен дизайн
- Линкбилдинг и контент-маркетинг — это маркетинговая работа
- Backend / база данных — сайт остаётся статическим
